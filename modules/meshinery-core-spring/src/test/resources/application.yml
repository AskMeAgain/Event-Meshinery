meshinery:
  core:
    batch-job: true
    grace-period-milliseconds: 20000
  connectors:
    kafka:
      bootstrap-servers: 'localhost:9092'
      group-id: 'asdztu1'

logging:
  level:
    org:
      apache:
        kafka:
          common:
            utils:
              AppInfoParser: WARN
          clients:
            producer:
              ProducerConfig: WARN
            consumer:
              ConsumerConfig: WARN
              KafkaConsumer: WARN
              internals:
                ConsumerCoordinator: WARN